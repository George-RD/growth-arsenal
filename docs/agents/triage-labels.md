# Triage labels

Only one triage role is required by the installed planning workflow:

| Role | Preferred GitHub label | Fallback when label is unavailable |
|---|---|---|
| Ready for an implementation agent | `ready-for-agent` | `Status: ready-for-agent` in the issue body |

Do not introduce a larger label taxonomy unless Growth Arsenal later adopts the `/triage` workflow.
